// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/python
{
	"name": "DMD_NatComm_2023",
	// Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
	"image": "mcr.microsoft.com/devcontainers/python:1-3.8-bullseye",
	
	// "runArgs": [
	// 	"--net", "host",
	// 	"-e", "DISPLAY=:0",
	// 	"-e", "XAUTHORITY=/tmp/.Xauthority",
	// 	"-v", "${localEnv:HOME}/.Xauthority:/tmp/.Xauthority"
	//   ],

	// Features to add to the dev container. More info: https://containers.dev/features.
	// "features": {},

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	// "forwardPorts": [],

	// Use 'postCreateCommand' to run commands after the container is created.
<<<<<<< HEAD
	"postCreateCommand": "pip3 install --upgrade pip && apt-get -y update && apt-get -y install build-essential cmake --no-install-recommends && pip3 install --user -r requirements.txt && pip install -U git+https://github.com/sichinaga/PyDMD.git@97b5aa2a8c488cb2336d15791c05a1408fb0bb15",
=======
	"postCreateCommand": "pip3 install --upgrade pip && apt-get -y update && apt-get -y install build-essential cmake --no-install-recommends && pip3 install --user -r requirements.txt && pip install -U git+https://github.com/sichinaga/PyDMD@97b5aa2a8c488cb2336d15791c05a1408fb0bb15",
>>>>>>> 5ae9192 (Massive code overhaul)
// Add the IDs of extensions you want installed when the container is created in the array below.
		// Configure properties specific to VS Code.
	"customizations": {
		"vscode": {
			// Add the IDs of extensions you want installed when the container is created.
			"extensions": [
				"ms-toolsai.jupyter",
				"ms-toolsai.vscode-jupyter-powertoys"
			]
		}
	},
	// Configure tool-specific properties.
	// "customizations": {},

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	"remoteUser": "root"
}